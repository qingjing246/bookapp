<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width,initial-scale=1,user-scalable=no">
    <title>Document</title>
</head>
<style>
    body {
        margin: 0;
        padding: 0;

    }
    a{
        text-decoration: none;
        color: #fff;
    }
    .subhead {
        height: 44px;
        line-height: 44px;
        font-size: 19px;
        display: -webkit-box;
        background: #333;
    }
    .booklist a{
        clear: both;
        line-height: 38px;
        height: 38px;
        padding: 0 20px 0 8px;
        font-size: 14px;
        border-bottom: 1px solid #dfdfdf;
        color: #4c4c4c;
        display: block;
        margin: 0;
        background-size: 138px;
        background-position: right -50px;
        text-overflow: ellipsis;
        white-space: nowrap;
        overflow: hidden;
    }
    .fle {
        display: -webkit-box;
        display: -webkit-flex;
        display: -ms-flexbox;
        display: flex;
    }

    .flex-fluid {
        -webkit-box-flex: 1;
        -moz-box-flex: 1;
        -webkit-flex: 1;
        -moz-flex: 1;
        -ms-flex: 1;
        flex: 1;
    }
    .shujia{
        text-align: right;
        padding: 0 10px 0 0;
    }
    .perv{
        padding: 0 0 0 10px;
    }
</style>
<body>
<div class="subhead fle">
    <a class="perv flex-fluid" href="/bookinfo/?id=<%= booklist[0]._doc.bookid%>">< 返回</a>
    <a >目录
        <span>(共有<%= booklist.length %>章)</span>
    </a>
    <a class="shujia flex-fluid" >书架</a>
</div>
<div class="booklist">
    <% for(i=0;i< 20;i++) { %>
    <a href="/read/?bookid=<%= booklist[i]._doc.bookid%>&id=<%= booklist[i]._doc.id%>"><%= booklist[i]._doc.title%></a>
    <% } %>
</div>
</body>
</html>